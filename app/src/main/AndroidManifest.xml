<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.managment.doctor.doctorappoinment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginregister.activities.LoginActivity" />
        <activity android:name=".loginregister.activities.RegisterActivity" />
        <activity android:name=".doc.PatientListActivity" />
        <activity android:name=".doc.DashBoard" />
        <activity android:name=".doc.AddPatientActivity" />
        <activity android:name=".imageauth.ImageAuthActivity" />
        <activity android:name=".imageauth.LoginImageActivity"></activity>
    </application>

</manifest>